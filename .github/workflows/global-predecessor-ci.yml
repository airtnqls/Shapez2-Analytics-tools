name: Global predecessor all-layer CI

on:
  push:
    branches: [optimize/latest-tmam-fastpath]
    paths:
      - backend/corner_half/global_predecessor.py
      - backend/corner_half/test_global_predecessor.py
      - scripts/search_pair_assembly_bottom_fix.py
      - scripts/search_build_pinable_shape2.py
      - .github/workflows/global-predecessor-ci.yml
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'push' && github.ref_name || github.event.pull_request.head.ref }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Compile and test global predecessor
        run: |
          python -m compileall -q backend/corner_half/global_predecessor.py scripts/search_build_pinable_shape2.py
          python -m unittest -v backend.corner_half.test_global_predecessor
      - name: Generate pair-assembly repair search
        run: |
          mkdir -p reports
          python scripts/search_pair_assembly_bottom_fix.py > reports/pair-assembly-bottom-fix.json
          python - <<'PY'
          import json
          data=json.load(open('reports/pair-assembly-bottom-fix.json',encoding='utf-8'))
          print({k:data[k] for k in ('exhaustiveTargets','solved','failed')})
          print('signatures',data['deltaSignatures'])
          print('family tail',data['highLayerFamily'][-4:])
          PY
      - name: Complete the legacy build_pinable_shape2 helper search
        run: |
          python scripts/search_build_pinable_shape2.py > reports/build-pinable-shape2-search.json
          python - <<'PY'
          import json
          data=json.load(open('reports/build-pinable-shape2-search.json',encoding='utf-8'))
          for report in data['reports']:
              print({k:report.get(k) for k in ('target','a1','a2','checked','craftable','halfStable','fullStable','skipped')})
              print('hits', len(report.get('hits', [])))
              for hit in report.get('hits', [])[:3]:
                  print(hit)
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: global-predecessor-all-layer
          path: |
            reports/pair-assembly-bottom-fix.json
            reports/build-pinable-shape2-search.json
          if-no-files-found: error
